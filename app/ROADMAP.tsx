/*const [blocs, setBlocs] = useState<BlocObject[]>(initialBlocs);

const updateBloc = (index: number, updatedBloc: BlocObject) => {
  setBlocs(prev =>
    prev.map((b, i) => (i === index ? updatedBloc : b))
  );
};

<Bloc
  bloc={blocs[index]}
  onChange={(updated) => updateBloc(index, updated)}
/>
*/
/*
export default function Bloc({
  bloc,
  onChange,
}: {
  bloc: BlocObject;
  onChange: (bloc: BlocObject) => void;
}) {
*/
//onChange(updatedBloc);
// setText === onChange(updatedBloc);
